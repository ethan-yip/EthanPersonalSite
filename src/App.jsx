// The new site entry: the yip-os desktop shell lives at "/".
// (The old homepage is preserved as src/legacy/LegacyApp.jsx, routed at /classic.)
import Desktop from './os/Desktop.jsx';

export default function App() {
  return <Desktop />;
}
